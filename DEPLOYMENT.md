# 🚀 РАЗВЕРТЫВАНИЕ БОТА НА PYTHONANYWHERE

## Шаг 1: Подготовка файлов

Убедись что у тебя есть эти файлы:
- `bot.py` - основной файл бота
- `solutions.py` - список смешных решений
- `requirements.txt` - зависимости

## Шаг 2: Создание аккаунта на PythonAnywhere

1. Перейди на https://www.pythonanywhere.com
2. Нажми "Sign up for free"
3. Создай аккаунт
4. Подтверди email
5. Логируйся

## Шаг 3: Загрузка файлов

### Вариант A: Через веб-интерфейс PythonAnywhere

1. Логируйся на https://www.pythonanywhere.com/user/
2. Перейди в "Files"
3. Создай новую папку: `/home/username/telegram_bot` (замени username на свой)
4. Загрузи туда файлы:
   - `bot.py`
   - `solutions.py`
   - `requirements.txt`

### Вариант B: Через Bash консоль (рекомендуется)

1. Логируйся на PythonAnywhere
2. Открой "Bash console"
3. Выполни команды:

```bash
# Перейди в домашнюю папку
cd ~

# Создай папку для бота
mkdir telegram_bot
cd telegram_bot

# Клонируй репозиторий ИЛИ загрузи файлы через Git
# Если у тебя есть GitHub репозиторий:
git clone https://github.com/твой_username/твой_репозиторий.git .

# Или просто вручную создай файлы (см. ниже)
```

## Шаг 4: Установка зависимостей

В Bash консоли PythonAnywhere:

```bash
cd ~/telegram_bot

# Создай виртуальное окружение
mkvirtualenv --python=/usr/bin/python3.10 telegram_bot

# Активируй его
workon telegram_bot

# Установи зависимости
pip install -r requirements.txt
```

## Шаг 5: Создание сценария Always-On Task

### Самый надежный способ: Always-On Task

1. Перейди в "Tasks" → "Always-on tasks"
2. Нажми "Create a new always-on task"
3. Введи путь: `/home/username/.virtualenvs/telegram_bot/bin/python /home/username/telegram_bot/bot.py`
4. Нажми "Create"

**Это самый простой способ! Бот будет работать 24/7**

## Шаг 6: Проверка работы

В Bash консоли:

```bash
cd ~/telegram_bot
workon telegram_bot

# Проверь что все работает
python bot.py
```

Если вывод показывает "🤖 БОТ ЗАПУЩЕН! Жми Ctrl+C для остановки." - ВСЕ РАБОТАЕТ!

Нажми Ctrl+C для остановки

## Шаг 7: Мониторинг

Перейди в "Tasks" и смотри статус своего task'а:
- **Green (Running)** - Бот работает ✅
- **Red (Stopped)** - Бот упал ❌

Если бот упал - нажми кнопку "Restart" и проверь логи

## 🔧 Альтернативный способ: Web App

Если Always-On Task не работает, используй Web App:

1. Перейди в "Web"
2. Нажми "Add a new web app"
3. Выбери "Manual configuration"
4. Выбери Python 3.10
5. В файле `/var/www/username_pythonanywhere_com_wsgi.py` добавь:

```python
import subprocess
import sys
import os

os.chdir('/home/username/telegram_bot')
sys.path.insert(0, '/home/username/telegram_bot')

# Запусти бота в фоне
subprocess.Popen([
    '/home/username/.virtualenvs/telegram_bot/bin/python',
    '/home/username/telegram_bot/bot.py'
])

def application(environ, start_response):
    status = '200 OK'
    response_headers = [('Content-type', 'text/plain')]
    start_response(status, response_headers)
    return [b'Bot is running!']
```

## ⚠️ Важно: Токен бота

**НЕ ЗАГРУЖАЙ ТОКЕН В ОТКРЫТЫЙ РЕПОЗИТОРИЙ!**

### Способ 1: Переменные окружения (рекомендуется)

В `bot.py` замени:

```python
TOKEN = "YOUR_BOT_TOKEN_HERE"
```

На:

```python
import os
TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
```

Затем в PythonAnywhere:

1. Перейди в "Web" → "Edit"
2. Найди строку `import os`
3. Добавь переменную окружения в Bash консоли:

```bash
echo "export TELEGRAM_BOT_TOKEN='твой_токен_здесь'" >> ~/.bashrc
source ~/.bashrc
```

### Способ 2: Файл конфигурации

Создай файл `config.py`:

```python
import os

TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', 'default_token_if_not_set')
```

В `bot.py`:

```python
from config import TOKEN
```

## 🔐 Безопасность

1. **Никогда** не выкладывай токен в публичный репозиторий
2. Используй переменные окружения
3. Проверяй логи регулярно
4. Если токен утек - создай новый через @BotFather

## 📊 Мониторинг и логирование

Логи бота доступны в:
- `/home/username/telegram_bot/bot.log` (если ты настроил логирование в файл)
- Консоль PythonAnywhere в разделе "Files" → "View"

Чтобы видеть логи в реальном времени:

```bash
cd ~/telegram_bot
tail -f bot.log
```

## 🐛 Решение проблем

### Бот не запускается

1. Проверь логи:
```bash
cd ~/telegram_bot
python bot.py
```

2. Проверь что все зависимости установлены:
```bash
workon telegram_bot
pip list | grep telegram
```

3. Проверь что токен правильный

4. Проверь интернет соединение PythonAnywhere

### Бот запускается, но не отвечает

1. Проверь что токен есть в переменных окружения
2. Перезагрузи task: "Tasks" → "Restart"
3. Проверь логи на ошибки

### Memory issues

Если PythonAnywhere говорит про memory limit:
1. Перейди в "Account" 
2. Апгрейдь аккаунт (платный план)
3. Или оптимизируй код (удали неиспользуемые решения)

## 💰 Стоимость

**Бесплатный план:**
- 512 MB RAM
- 100 MHz CPU
- 1 always-on task (если есть)
- До 100 000 веб-запросов в день

**Платные планы:**
- Начиная от $5/месяц
- Больше RAM и CPU
- Больше файловых операций

Для простого бота бесплатного плана **более чем достаточно**!

## ✅ Финальная проверка

После развертывания:

1. ✅ Бот включен (зеленый статус в Tasks)
2. ✅ Отправь тестовое сообщение боту в Telegram
3. ✅ Получи ответ с одним из 600+ смешных решений
4. ✅ ГОТОВО! 🎉

---

## 🆘 Быстрая помощь

**Команды которые помогут:**

```bash
# Активируй окружение
workon telegram_bot

# Установи пакеты
pip install python-telegram-bot

# Запусти бота
python ~/telegram_bot/bot.py

# Посмотри логи
tail -f ~/telegram_bot/bot.log

# Перезагрузи task
# (в веб-интерфейсе нажми Restart)
```

---

**Удачи с развертыванием! Бот готов к боевым условиям!** 🚀
